import BlogCard from "../components/BlogCard";

const Home = () => {
  // toastWarn("Success !");

  // console.log(data);

  return (
    <div>
      <BlogCard />
    </div>
  );
};

export default Home;
